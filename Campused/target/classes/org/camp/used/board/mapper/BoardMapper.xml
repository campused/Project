<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="org.camp.used.board.mapper.BoardMapper">
 
	<insert id="insert"> 
		insert into tbl_board (bstate, btitle, bcontent, bprice, bdeleted, breg_date, bmod_date, member_mno)
		values (#{bstate}, #{btitle}, #{bcontent}, #{bprice}, #{bdeleted}, now(), now(), #{memberMno})
	</insert>
 
 </mapper>